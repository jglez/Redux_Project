// Initial State Values
const initialState = {
  name: '',
  image: '',
  isFetching: false,
  error: ''
}

// Pass initial state values into reducer
export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}