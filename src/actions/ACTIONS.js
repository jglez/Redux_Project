export const getChar = () => {
  // 1. set isFetching = true
  // 2. set off our API call
  // 3. if our API call is successful, add res to char
  // 4. render name
  // 5. render image
  // 6. if our API call is unsuccessful, add error message to error action

}